#include <iostream>
#include <string>
#include <vector>
#include "settings.h"

int main() {
  Settings settings("transfer.info", "me.info");

  // std::vector<std::string> lines;
  // std::string line;
  // while (std::getline(file, line)) {
  //   lines.push_back(line);
  // }
  // file.close();

  // address = lines[0];
  // name = lines[1];
  // file_name = lines[2];

  // file.open("me.info", std::fstream::in);
  // if (file.good()) {
  //   lines.clear();
  //   while (std::getline(file, line)) {
  //     lines.push_back(line);
  //   }
  //   file.close();

  //   name = lines[0];
  //   id = lines[1];
  //   key = lines[2];
  
  // send relogin request
  

  return 0;
}